export const firstName = (state: any) => state.userData.firstName;
export const lastName = (state: any) => state.userData.lastName;
export const age = (state: any) => state.userData.age;
export const email = (state: any) => state.userData.email;
export const phone = (state: any) => state.userData.phone;
